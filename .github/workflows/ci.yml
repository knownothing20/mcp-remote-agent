name: AgentPort CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: agentport-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  privacy:
    name: Privacy Guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm run privacy:check
      - run: npm run test:privacy
  architecture:
    name: Architecture (${{ matrix.os }}, Node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - run: node test/test-architecture.cjs
      - run: node test/test-daemon-gateway.cjs
      - run: node test/test-exec-core.cjs
      - run: node test/test-client-runtime.cjs
      - run: node test/test-client-development-sessions.cjs
      - run: node test/test-project-lock.cjs
      - run: node test/test-development-session-service.cjs
      - run: node test/test-development-gateway.cjs
      - run: node test/test-client-redaction.cjs
      - run: node test/test-daemon-response-redaction.cjs
      - run: node test/test-named-workspaces.cjs

  windows-jobs:
    name: Job Service (windows-latest, Node ${{ matrix.node }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - run: node test/test-job-service.cjs
      - run: node test/test-daemon-exec-jobs.cjs
      - run: node test/test-daemon-exec-jobs.cjs

  durable-linux:
    name: Legacy, durable jobs, Worktrees, and MCP V3 (Ubuntu, Node 22)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run test:lifecycle
      - run: node test/test-job-service.cjs
      - run: node test/test-job-worker-readiness.cjs
      - run: node test/test-daemon-exec-jobs.cjs
      - run: node test/test-project-lock.cjs
      - run: node test/test-development-session-service.cjs
      - run: node test/test-development-gateway.cjs
      - run: node test/test-client-mcp-v3.cjs
      - run: node test/test-mcp-redaction.cjs
      - run: node test/test-daemon-response-redaction.cjs
      - run: npm audit --audit-level=moderate
      - name: Clean-install and verify legacy daemon dependencies
        run: |
          npm --prefix server ci --ignore-scripts --no-audit --no-fund
          node -e "const v=require('./server/node_modules/qs/package.json').version;if(v!=='6.15.3')throw new Error('Expected qs 6.15.3, got '+v)"
      - run: npm --prefix server audit --audit-level=moderate
